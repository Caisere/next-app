export const metadata = {
    title: "About Us",
    description: "About Us Page",
};

export default function About({ children }) {
    return (
        <div>
            {children}
        </div>
    )
}
