

export const metadata = {
    title: "About Us",
    description: "About Us Page",
};

export default function About({ children }) {
    return (
        <html lang="en">
            <body>
                {children}
            </body>
        </html>
    );
}
